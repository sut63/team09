// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/team09/app/ent/office"
	"github.com/team09/app/ent/predicate"
	"github.com/team09/app/ent/speacial_doctor"
)

// SpeacialDoctorUpdate is the builder for updating Speacial_doctor entities.
type SpeacialDoctorUpdate struct {
	config
	hooks      []Hook
	mutation   *SpeacialDoctorMutation
	predicates []predicate.Speacial_doctor
}

// Where adds a new predicate for the builder.
func (sdu *SpeacialDoctorUpdate) Where(ps ...predicate.Speacial_doctor) *SpeacialDoctorUpdate {
	sdu.predicates = append(sdu.predicates, ps...)
	return sdu
}

// SetName sets the name field.
func (sdu *SpeacialDoctorUpdate) SetName(s string) *SpeacialDoctorUpdate {
	sdu.mutation.SetName(s)
	return sdu
}

// AddOfficeIDs adds the offices edge to Office by ids.
func (sdu *SpeacialDoctorUpdate) AddOfficeIDs(ids ...int) *SpeacialDoctorUpdate {
	sdu.mutation.AddOfficeIDs(ids...)
	return sdu
}

// AddOffices adds the offices edges to Office.
func (sdu *SpeacialDoctorUpdate) AddOffices(o ...*Office) *SpeacialDoctorUpdate {
	ids := make([]int, len(o))
	for i := range o {
		ids[i] = o[i].ID
	}
	return sdu.AddOfficeIDs(ids...)
}

// Mutation returns the SpeacialDoctorMutation object of the builder.
func (sdu *SpeacialDoctorUpdate) Mutation() *SpeacialDoctorMutation {
	return sdu.mutation
}

// RemoveOfficeIDs removes the offices edge to Office by ids.
func (sdu *SpeacialDoctorUpdate) RemoveOfficeIDs(ids ...int) *SpeacialDoctorUpdate {
	sdu.mutation.RemoveOfficeIDs(ids...)
	return sdu
}

// RemoveOffices removes offices edges to Office.
func (sdu *SpeacialDoctorUpdate) RemoveOffices(o ...*Office) *SpeacialDoctorUpdate {
	ids := make([]int, len(o))
	for i := range o {
		ids[i] = o[i].ID
	}
	return sdu.RemoveOfficeIDs(ids...)
}

// Save executes the query and returns the number of rows/vertices matched by this operation.
func (sdu *SpeacialDoctorUpdate) Save(ctx context.Context) (int, error) {
	if v, ok := sdu.mutation.Name(); ok {
		if err := speacial_doctor.NameValidator(v); err != nil {
			return 0, &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}

	var (
		err      error
		affected int
	)
	if len(sdu.hooks) == 0 {
		affected, err = sdu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SpeacialDoctorMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			sdu.mutation = mutation
			affected, err = sdu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(sdu.hooks) - 1; i >= 0; i-- {
			mut = sdu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sdu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (sdu *SpeacialDoctorUpdate) SaveX(ctx context.Context) int {
	affected, err := sdu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (sdu *SpeacialDoctorUpdate) Exec(ctx context.Context) error {
	_, err := sdu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sdu *SpeacialDoctorUpdate) ExecX(ctx context.Context) {
	if err := sdu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (sdu *SpeacialDoctorUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   speacial_doctor.Table,
			Columns: speacial_doctor.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: speacial_doctor.FieldID,
			},
		},
	}
	if ps := sdu.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := sdu.mutation.Name(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: speacial_doctor.FieldName,
		})
	}
	if nodes := sdu.mutation.RemovedOfficesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   speacial_doctor.OfficesTable,
			Columns: []string{speacial_doctor.OfficesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: office.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := sdu.mutation.OfficesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   speacial_doctor.OfficesTable,
			Columns: []string{speacial_doctor.OfficesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: office.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, sdu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{speacial_doctor.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// SpeacialDoctorUpdateOne is the builder for updating a single Speacial_doctor entity.
type SpeacialDoctorUpdateOne struct {
	config
	hooks    []Hook
	mutation *SpeacialDoctorMutation
}

// SetName sets the name field.
func (sduo *SpeacialDoctorUpdateOne) SetName(s string) *SpeacialDoctorUpdateOne {
	sduo.mutation.SetName(s)
	return sduo
}

// AddOfficeIDs adds the offices edge to Office by ids.
func (sduo *SpeacialDoctorUpdateOne) AddOfficeIDs(ids ...int) *SpeacialDoctorUpdateOne {
	sduo.mutation.AddOfficeIDs(ids...)
	return sduo
}

// AddOffices adds the offices edges to Office.
func (sduo *SpeacialDoctorUpdateOne) AddOffices(o ...*Office) *SpeacialDoctorUpdateOne {
	ids := make([]int, len(o))
	for i := range o {
		ids[i] = o[i].ID
	}
	return sduo.AddOfficeIDs(ids...)
}

// Mutation returns the SpeacialDoctorMutation object of the builder.
func (sduo *SpeacialDoctorUpdateOne) Mutation() *SpeacialDoctorMutation {
	return sduo.mutation
}

// RemoveOfficeIDs removes the offices edge to Office by ids.
func (sduo *SpeacialDoctorUpdateOne) RemoveOfficeIDs(ids ...int) *SpeacialDoctorUpdateOne {
	sduo.mutation.RemoveOfficeIDs(ids...)
	return sduo
}

// RemoveOffices removes offices edges to Office.
func (sduo *SpeacialDoctorUpdateOne) RemoveOffices(o ...*Office) *SpeacialDoctorUpdateOne {
	ids := make([]int, len(o))
	for i := range o {
		ids[i] = o[i].ID
	}
	return sduo.RemoveOfficeIDs(ids...)
}

// Save executes the query and returns the updated entity.
func (sduo *SpeacialDoctorUpdateOne) Save(ctx context.Context) (*Speacial_doctor, error) {
	if v, ok := sduo.mutation.Name(); ok {
		if err := speacial_doctor.NameValidator(v); err != nil {
			return nil, &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}

	var (
		err  error
		node *Speacial_doctor
	)
	if len(sduo.hooks) == 0 {
		node, err = sduo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SpeacialDoctorMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			sduo.mutation = mutation
			node, err = sduo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(sduo.hooks) - 1; i >= 0; i-- {
			mut = sduo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sduo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (sduo *SpeacialDoctorUpdateOne) SaveX(ctx context.Context) *Speacial_doctor {
	sd, err := sduo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return sd
}

// Exec executes the query on the entity.
func (sduo *SpeacialDoctorUpdateOne) Exec(ctx context.Context) error {
	_, err := sduo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sduo *SpeacialDoctorUpdateOne) ExecX(ctx context.Context) {
	if err := sduo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (sduo *SpeacialDoctorUpdateOne) sqlSave(ctx context.Context) (sd *Speacial_doctor, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   speacial_doctor.Table,
			Columns: speacial_doctor.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: speacial_doctor.FieldID,
			},
		},
	}
	id, ok := sduo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing Speacial_doctor.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := sduo.mutation.Name(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: speacial_doctor.FieldName,
		})
	}
	if nodes := sduo.mutation.RemovedOfficesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   speacial_doctor.OfficesTable,
			Columns: []string{speacial_doctor.OfficesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: office.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := sduo.mutation.OfficesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   speacial_doctor.OfficesTable,
			Columns: []string{speacial_doctor.OfficesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: office.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	sd = &Speacial_doctor{config: sduo.config}
	_spec.Assign = sd.assignValues
	_spec.ScanValues = sd.scanValues()
	if err = sqlgraph.UpdateNode(ctx, sduo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{speacial_doctor.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return sd, nil
}
